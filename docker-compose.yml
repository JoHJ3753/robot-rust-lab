services:
  rust-dev:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: robot-rust-dev

    working_dir: /workspace

    volumes:
      - ./workspace:/workspace

    tty: true
    stdin_open: true

    command: bash